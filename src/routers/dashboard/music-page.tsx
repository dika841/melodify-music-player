import { FC, ReactElement } from "react"
import { Header } from "./modules"

export const MusicPage:FC = ():ReactElement => {
    return (
        <section>
            <Header/>
            Music Dashboard
        </section>
    )
}